<h1>Products</h1>

<%= link_to 'New Product', new_product_path %>

<div id="products">
    <% @products.each do |product| %>
        <div>
            <%= link_to product.name, product if authenticated? %>
        </div>
    <% end %>
</div>
